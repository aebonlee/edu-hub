# 개발일지 — 2026-03-17

## 종합 구현 — Shop, 관리자 대시보드, 진로컨설팅, 로그인 게이트, README

---

## 1. Shop 페이지 신설 (/shop)
- **새 파일**: `src/pages/Shop.jsx`
- **수정 파일**: `src/layouts/PublicLayout.jsx`
- **내용**: 상품 목록 페이지 생성 — Cart.jsx가 `/shop` 링크를 참조하지만 페이지가 없던 문제 해결
- **기능**:
  - `productStorage.getProducts()`로 Supabase 상품 로드
  - 카테고리 필터 탭 (전체/도서/전자출판/간행물/강좌)
  - 상품명 검색 기능
  - 상품 카드 그리드 (기존 `.shop-grid`, `.product-card` CSS 활용)
  - "장바구니 담기" 버튼 → `CartContext.addItem()` 연동
  - 관리자: 수정/삭제/품절전환 버튼 인라인 표시
- **라우트**: `/shop` → `<Shop />` (PublicLayout에 lazy import 추가)
- **번역 키 추가**: `shop.search`, `shop.searchPlaceholder`, `shop.edit`, `shop.delete` (ko/en)
- **CSS 변경**: `site.css`에서 `.cart-icon-link { display: none }` 제거 → 장바구니 아이콘 활성화

## 2. 관리자 대시보드 신설 (/admin/*)
### 2-1. 기반 컴포넌트
- **새 파일**: `src/components/AdminGuard.jsx` — `isAdmin` 체크, 비관리자 `/`로 리다이렉트
- **새 파일**: `src/utils/adminApi.js` — 관리자 API 유틸리티
  - `getDashboardStats()` — 주문수, 매출, 회원수, 상품수 통계
  - `getAllOrders(page, limit, status)` — 전체 주문 조회 (order_items 조인)
  - `updateOrderStatus(orderId, status)` — 주문 상태 변경
  - `getAllMembers(page, limit, search)` — 전체 회원 조회 + 검색
  - `updateMemberStatus(userId, status, reason)` — 회원 상태 변경
- **새 파일**: `src/styles/admin.css` — 관리자 전용 스타일
  - 사이드바 레이아웃 (260px 고정, 모바일 슬라이드)
  - 통계 카드 (4열 그리드, 아이콘 색상 4종)
  - 테이블 (헤더/바디/호버), 상태 뱃지 (paid/pending/cancelled/refunded/active/banned)
  - 모달, 폼, 페이지네이션, 빈 상태
  - 다크모드 `[data-theme="dark"]` 오버라이드
  - 반응형 (768px 이하 사이드바 토글)

### 2-2. 레이아웃 & 라우팅
- **새 파일**: `src/layouts/AdminLayout.jsx` — 관리자 레이아웃
  - AdminGuard 래핑, 사이드바 4개 메뉴 (대시보드/주문/회원/상품)
  - "사이트로 돌아가기" 링크
  - 모바일 사이드바 토글 버튼
- **수정**: `src/App.jsx` — 라우트 분리
  - `/admin/*` → `AdminLayout` (lazy import + Suspense)
  - `*` → `PublicLayout` (기존)

### 2-3. 대시보드 (/admin)
- **새 파일**: `src/pages/admin/Dashboard.jsx`
- 4개 통계 카드: 총 주문, 총 매출, 총 회원, 활성 상품

### 2-4. 주문 관리 (/admin/orders)
- **새 파일**: `src/pages/admin/Orders.jsx`
- 주문 목록 테이블 + 상태 필터 (전체/결제완료/대기/취소/환불)
- 상태 변경 드롭다운 (인라인)
- 주문 상세 펼치기 (order_items 테이블)
- 페이지네이션 (20건 단위)

### 2-5. 회원 관리 (/admin/members)
- **새 파일**: `src/pages/admin/Members.jsx`
- 회원 목록 + 이름/이메일 검색
- 상태 변경 모달 (활성/정지/차단 + 사유 입력)
- 페이지네이션 (20건 단위)

### 2-6. 상품 관리 (/admin/products)
- **새 파일**: `src/pages/admin/Products.jsx`
- 상품 목록 테이블 (비활성 상품 포함, 반투명 표시)
- 수정/품절전환/삭제(비활성화) 기능
- "상품 등록" 버튼 → ProductForm 이동

### 2-7. 상품 등록/수정 폼
- **새 파일**: `src/pages/admin/ProductForm.jsx`
- 등록(`/admin/products/new`) + 수정(`/admin/products/edit/:id`) 공용
- 필드: slug, 카테고리, 상품명(ko/en), 설명(ko/en), 가격, 이미지 URL, 정렬순서, 품절여부
- `createProduct()` / `updateProduct()` 사용

### 2-8. Navbar 관리자 링크 내부화
- **수정**: `src/components/layout/Navbar.jsx`
- 기존: `<a href={site.parentSite.url + '/admin'}>`(외부 링크)
- 변경: `<Link to="/admin">`(내부 라우트) + `t('auth.admin')` 번역 사용

## 3. 진로컨설팅 카테고리 신설

### 3-1. site.js 설정
- **수정**: `src/config/site.js`
- `categories` 배열에 `{ id: 'career', name: '진로컨설팅', icon: 'fa-solid fa-user-tie' }` 추가
- `menuItems`에 진로컨설팅 메뉴 + 드롭다운 3개 (자격증학습 다음, 학습사이트분양 앞)
- `footerLinks`에 진로컨설팅 추가
- `learningSites`에 3개 서비스 카드 추가:

| 서비스 | isService | price | 설명 |
|--------|-----------|-------|------|
| 맞춤 커리어 사이트 제작 | true | 50,000 | 포트폴리오 웹사이트 맞춤 제작 |
| 1:1 커리어 코칭 | true | 0 | 전문 컨설턴트 진로 상담 (가격 문의) |
| 이력서 & 자기소개서 코칭 | true | 0 | 이력서·자소서 첨삭 (가격 문의) |

### 3-2. 번역 키
- **수정**: `src/utils/translations.js`
- 네비게이션: `site.nav.career`, `site.nav.careerSite`, `site.nav.careerCoaching`, `site.nav.resumeCoaching`
- 홈: `site.home.categoryCareerDesc`
- 코스: `site.courses.categoryTitle.career`, `site.courses.addToCart`, `site.courses.contactForPrice`, `site.courses.priceFrom`, `site.courses.loginRequired`
- 영어 키 동일 추가

### 3-3. Courses.jsx 서비스 카드 지원
- **수정**: `src/pages/Courses.jsx`
- `isService` 플래그 감지 → 가격 표시 + 장바구니 담기 또는 가격 문의 버튼
- `price > 0`: 가격 + "장바구니 담기" (CartContext.addItem)
- `price === 0`: "가격 문의" (분양 페이지 링크)
- `.edu-service-purchase` / `.edu-service-price` CSS 추가 (`site.css`)

### 3-4. Home.jsx 카테고리 설명
- **수정**: `src/pages/Home.jsx`
- `CATEGORY_DESC_KEYS`에 `'career': 'site.home.categoryCareerDesc'` 추가

## 4. 로그인 필수 링크 (Login Gate)

### 4-1. Courses.jsx 외부 링크 게이팅
- **수정**: `src/pages/Courses.jsx`
- 기존 `<a href={ls.url}>` → `<button onClick={handleVisitSite}>`
- `handleVisitSite(url)`:
  - 미로그인 → sessionStorage에 returnUrl 저장 → `/login`으로 리다이렉트
  - 로그인 상태 → `window.open(url, '_blank')` 실행

### 4-2. Login.jsx returnUrl 처리
- **수정**: `src/pages/Login.jsx`
- `location.state.returnUrl` 수신
- 이메일 로그인 성공 시 returnUrl이 있으면 `window.open(returnUrl)` 후 페이지 이동
- OAuth 콜백 후: `isLoggedIn` 감지 시 sessionStorage의 `dreamit_return_url` 확인 → 외부 사이트 열기
- sessionStorage 사용으로 OAuth 리다이렉트 시에도 returnUrl 유지

## 5. README.md 재작성
- **백업**: `README.md` → `README_backup.md`
- **재작성**: `README.md`
  - 7개 카테고리, 34개 학습사이트/서비스 현행화
  - 주요 기능 (다국어, 다크모드, 장바구니, 결제, 관리자, 로그인 게이트)
  - 프로젝트 구조 (admin, shop, layouts 포함)
  - 기술 스택, 빠른 시작 가이드, 빌드 & 배포

## 6. 구현 계획 문서화
- **새 파일**: `docs/implementation-plan.md` — 전체 구현 계획서 (6개 Phase)

---

## 최종 메뉴 구조

| 카테고리 | 항목 |
|---------|------|
| 교양분야 | 컴퓨팅 사고, 통계학, 영어, 글쓰기, 프레젠테이션 |
| 인공지능활용 | 프롬프트 엔지니어링, LLM & 랭체인, 업무자동화, AI 데이터 분석, AI 이미지 생성 |
| 경영전공 | 회계학, 경영전략론, 재무관리, 마케팅개론, 셀프브랜딩, UX디자인, 디지털비즈니스 |
| 컴퓨터전공 | DB 학습, 알고리즘, 자료구조, 정보통신, 소프트웨어설계 & 구현 |
| 코딩학습 | HTML 학습, Web 학습, React 학습, 코딩 학습 |
| 자격증학습 | 정보처리기사, 컴퓨터활용능력, SQLD, 리눅스마스터, 경영정보시각화 |
| **진로컨설팅** | 맞춤 커리어 사이트 제작, 1:1 커리어 코칭, 이력서 & 자기소개서 코칭 |
| 학습사이트분양 | (별도 페이지) |

---

## 변경 파일 목록

### 새 파일 (12개)
| 파일 | 용도 |
|------|------|
| `src/components/AdminGuard.jsx` | 관리자 라우트 보호 |
| `src/utils/adminApi.js` | 관리자 API 유틸리티 |
| `src/styles/admin.css` | 관리자 대시보드 스타일 |
| `src/pages/Shop.jsx` | 상품 목록 페이지 |
| `src/layouts/AdminLayout.jsx` | 관리자 레이아웃 |
| `src/pages/admin/Dashboard.jsx` | 관리자 대시보드 |
| `src/pages/admin/Orders.jsx` | 주문 관리 |
| `src/pages/admin/Members.jsx` | 회원 관리 |
| `src/pages/admin/Products.jsx` | 상품 관리 |
| `src/pages/admin/ProductForm.jsx` | 상품 등록/수정 폼 |
| `docs/개발일지_2026-03-17.md` | 개발일지 |
| `docs/implementation-plan.md` | 구현 계획 문서 |

### 수정 파일 (11개)
| 파일 | 변경 내용 |
|------|----------|
| `src/App.jsx` | `/admin/*` 라우트 분리 (AdminLayout lazy load) |
| `src/layouts/PublicLayout.jsx` | `/shop` 라우트 추가 |
| `src/config/site.js` | career 카테고리, 메뉴, 푸터, 3개 서비스 카드 |
| `src/utils/translations.js` | career, shop, courses 번역 키 (ko/en) |
| `src/pages/Courses.jsx` | 서비스 카드 지원 + 로그인 게이트 |
| `src/pages/Home.jsx` | career 카테고리 설명 키 |
| `src/pages/Login.jsx` | returnUrl 처리 (sessionStorage + state) |
| `src/components/layout/Navbar.jsx` | 관리자 링크 내부화 (`<Link to="/admin">`) |
| `src/index.css` | `admin.css` import 추가 |
| `src/styles/site.css` | 서비스 구매 CSS + 장바구니 아이콘 활성화 |
| `README.md` | 전면 재작성 (7개 카테고리, 34개 사이트, 기능 현행화) |

### 기타
| 파일 | 용도 |
|------|------|
| `README_backup.md` | 기존 README 백업 |

---

## 7. 네비게이션 액션 버튼 통합 (풍선 도움말) + 메뉴 간격 확대

### 배경
`.nav-actions`에 5개 버튼(돋보기/장바구니/EN/팔레트/해달)이 나란히 배치되어 공간을 과다 점유하고, 메뉴 텍스트 간격이 좁았음.

### 변경 내용

#### 7-1. Navbar.jsx — 5개 버튼 → 1개 ⚙️ 버튼 통합
- **수정**: `src/components/layout/Navbar.jsx`
- `showColorPicker` state → `showToolbar` state로 대체
- 5개 개별 버튼(검색, 장바구니, 언어, 컬러피커, 테마토글) 제거
- 새 `.nav-toolbar-wrapper` 추가: ⚙️(gear) 아이콘 버튼 1개 + 풍선 tooltip
- 풍선 내부 구성 (세로 배치):

| 항목 | 아이콘 | 동작 |
|------|--------|------|
| 검색 | 🔍 | SearchModal 열기 |
| 장바구니 | 🛒 | `/cart` 이동 + 뱃지 표시 |
| 언어 전환 | 🌐 | EN/KR 토글 |
| 테마 색상 | 🎨 | 5개 컬러 dot 인라인 표시 |
| 화면 모드 | ☀️/🌙/🔄 | Light → Dark → Auto 순환 |

- 오버레이 클릭 시 닫기 (기존 컬러피커 오버레이 패턴 재사용)
- 라우트 변경 시 자동 닫힘
- `.nav-actions`에는 ⚙️ 버튼 + 유저 아바타/Login만 남김

#### 7-2. navbar.css — 메뉴 간격 확대 + 풍선 도움말 스타일
- **수정**: `src/styles/navbar.css`
- **메뉴 간격 확대**:
  - `.nav-menu { gap: 0 }` → `gap: 8px`
  - `.nav-link { padding: 12px 14px }` → `padding: 12px 20px`
  - 밑줄 위치도 `left/right: 14px` → `20px`으로 동기화
- **풍선 도움말 CSS 추가**:
  - `.nav-toolbar-wrapper` — `position: relative`
  - `.nav-toolbar-btn` — 40×40, 기존 버튼 스타일 패턴
  - `.nav-toolbar-tooltip` — `.nav-user-dropdown` 패턴 (글래스모피즘, 화살표, 애니메이션)
  - `.nav-toolbar-item` — 각 행 (아이콘 + 라벨 + 컨트롤), hover 효과
  - `.nav-toolbar-overlay` — 투명 오버레이 (클릭 시 닫기)
  - `.nav-toolbar-colors` — 5개 컬러 dot 인라인
  - `.nav-toolbar-badge` — 장바구니 카운트 뱃지
  - `.nav-toolbar-value` — 오른쪽 값 표시 (EN/KR, 모드 이모지)
- **반응형 (768px 이하)**: 기존 개별 버튼 축소 코드 제거, 풍선 위치 조정, 메뉴 gap/padding 원래 값으로 리셋

#### 7-3. dark-mode.css — 다크모드 풍선 스타일
- **수정**: `src/styles/dark-mode.css`
- `.nav-toolbar-tooltip` — 어두운 배경 + 어두운 테두리 + 짙은 그림자
- `.nav-toolbar-arrow` — 어두운 배경
- `.nav-toolbar-item` — 밝은 텍스트 + hover 시 블루 하이라이트
- `.nav-toolbar-colors-row` — hover 배경 투명 유지
- `.nav-toolbar-value` — 회색 텍스트

### 수정 파일 (3개)
| 파일 | 변경 내용 |
|------|----------|
| `src/components/layout/Navbar.jsx` | 5개 버튼 → 1개 ⚙️ 버튼 + 풍선 tooltip |
| `src/styles/navbar.css` | 메뉴 간격 확대 + 풍선 도움말 스타일 + 반응형 정리 |
| `src/styles/dark-mode.css` | 풍선 tooltip 다크모드 오버라이드 |

---

## 8. "학습사이트분양" → "커뮤니티" 드롭다운 메뉴 전환

### 배경
상단 네비게이션의 "학습사이트분양" 메뉴가 단일 링크(`/franchise`)로 존재했으나,
커뮤니티 성격의 하위 페이지들을 묶어 드롭다운 메뉴로 전환할 필요가 있었음.

### 변경 내용

#### 8-1. site.js — 메뉴 + 푸터 변경
- **수정**: `src/config/site.js`
- `menuItems` 마지막 항목: `{ path: '/franchise', labelKey: 'site.nav.franchise' }` (단일 링크)
  → `{ labelKey: 'site.nav.community', path: '/about', dropdown: [...] }` (드롭다운)
- 드롭다운 하위 메뉴 4개:

| 라벨키 | 경로 | 설명 |
|--------|------|------|
| `site.nav.aboutHub` | `/about` | Edu Hub 소개 |
| `site.nav.notice` | `/notice` | 공지사항 (새 페이지) |
| `site.nav.franchiseInquiry` | `/franchise` | 학습사이트 분양문의 (기존 페이지) |
| `site.nav.qna` | `/qna` | Q&A (새 페이지) |

- `footerLinks`: `{ path: '/franchise', labelKey: 'site.nav.franchise' }` → `{ path: '/about', labelKey: 'site.nav.community' }`

#### 8-2. translations.js — 번역 키 추가
- **수정**: `src/utils/translations.js`
- **한국어 nav 섹션** 추가:
  - `community: '커뮤니티'`, `aboutHub: 'Edu Hub 소개'`, `notice: '공지사항'`
  - `franchiseInquiry: '학습사이트 분양문의'`, `qna: 'Q&A'`
- **영어 nav 섹션** 추가:
  - `community: 'Community'`, `aboutHub: 'About Edu Hub'`, `notice: 'Notice'`
  - `franchiseInquiry: 'Site Franchise Inquiry'`, `qna: 'Q&A'`
- **한국어/영어 site 섹션** — 새 페이지용:
  - `site.notice.title/subtitle/comingSoon`
  - `site.qna.title/subtitle/comingSoon`

#### 8-3. Notice.jsx — 공지사항 페이지 (신규)
- **새 파일**: `src/pages/Notice.jsx`
- About.jsx 패턴 따라: SEOHead + page-header + coming-soon 안내
- 아이콘: `fa-solid fa-bullhorn`

#### 8-4. QnA.jsx — Q&A 페이지 (신규)
- **새 파일**: `src/pages/QnA.jsx`
- 동일 패턴, 아이콘: `fa-solid fa-circle-question`

#### 8-5. PublicLayout.jsx — 라우트 추가
- **수정**: `src/layouts/PublicLayout.jsx`
- `Notice`, `QnA` lazy import 추가
- `<Route path="/notice" element={<Notice />} />`, `<Route path="/qna" element={<QnA />} />` 추가

#### 8-6. Navbar.jsx — isActive 개선
- **수정**: `src/components/layout/Navbar.jsx`
- 기존 `isActive`는 `activePath || path`의 `startsWith`만 체크
- 변경: 드롭다운 하위 경로에서도 부모 메뉴 활성화
  ```js
  if (item.dropdown) {
    return item.dropdown.some((sub) => location.pathname.startsWith(sub.path));
  }
  ```
- `/notice`, `/qna`, `/franchise` 방문 시 "커뮤니티" 메뉴 하이라이트

#### 8-7. site.css — coming-soon 페이지 스타일
- **수정**: `src/styles/site.css`
- `.edu-coming-soon-section` — 상하 여백
- `.edu-coming-soon-card` — 중앙 정렬 (max-width: 480px)
- `.edu-coming-soon-icon` — 88px 원형 배경 + 36px 아이콘
- 다크모드: `[data-theme="dark"] .edu-coming-soon-icon` 오버라이드

### 수정/생성 파일 (7개)

| 파일 | 변경 |
|------|------|
| `src/config/site.js` | franchise 단일 → community 드롭다운, 푸터 링크 변경 |
| `src/utils/translations.js` | nav 번역 5개 + page 번역 6개 (ko/en) |
| `src/pages/Notice.jsx` | **신규** — 공지사항 coming-soon 페이지 |
| `src/pages/QnA.jsx` | **신규** — Q&A coming-soon 페이지 |
| `src/layouts/PublicLayout.jsx` | Notice, QnA lazy import + 라우트 추가 |
| `src/components/layout/Navbar.jsx` | isActive 드롭다운 하위 경로 체크 추가 |
| `src/styles/site.css` | coming-soon 페이지 스타일 + 다크모드 |

### 최종 네비게이션 구조

| 메뉴 | 하위 |
|------|------|
| 교양분야 | 컴퓨팅 사고, 통계학, 영어, 글쓰기, 프레젠테이션 |
| 인공지능활용 | 프롬프트 엔지니어링, LLM & 랭체인, 업무자동화, AI 데이터 분석, AI 이미지 생성 |
| 경영전공 | 회계학, 경영전략론, 재무관리, 마케팅개론, 셀프브랜딩, UX디자인, 디지털비즈니스 |
| 컴퓨터전공 | DB 학습, 알고리즘, 자료구조, 정보통신, 소프트웨어설계 & 구현 |
| 코딩학습 | HTML 학습, Web 학습, React 학습, 코딩 학습 |
| 자격증학습 | 정보처리기사, 컴퓨터활용능력, SQLD, 리눅스마스터, 경영정보시각화 |
| 진로컨설팅 | 맞춤 커리어 사이트 제작, 1:1 커리어 코칭, 이력서 & 자기소개서 코칭 |
| **커뮤니티** | **Edu Hub 소개, 공지사항, 학습사이트 분양문의, Q&A** |
