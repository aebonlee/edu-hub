# 개발일지 — 2026-03-16

## 1. 자료구조 박스 링크 수정
- **파일**: `src/config/site.js`
- **내용**: 자료구조(data-structure) 과목 카드의 `url`을 `#` → `https://db-study.dreamitbiz.com/`로 변경
- **사유**: 자료구조 학습 사이트 오픈에 따른 실제 URL 연결

## 2. 상단 네비게이션 메뉴 개편
- **파일**: `src/config/site.js`, `src/utils/translations.js`
- **변경 전**: 홈 | 교양분야 | 경영전공분야 | 컴퓨터전공분야 | 자격증학습분야 | 학습사이트분양
- **변경 후**: 교양분야 | 경영전공 | 컴퓨터전공 | 코딩학습 | 자격증학습 | 학습사이트분양
- **세부 내용**:
  - `홈` 메뉴 제거
  - 메뉴명에서 '분야' 접미사 제거 (경영전공, 컴퓨터전공, 자격증학습)
  - `코딩학습` 카테고리 신규 추가
  - 컴퓨터전공에서 HTML 학습, Web 학습, React 학습을 코딩학습으로 이동
  - 컴퓨터전공 하위: DB 학습, 알고리즘, 자료구조
  - 코딩학습 하위: HTML 학습, Web 학습, React 학습, 코딩 학습(외부 링크)
  - 한/영 번역 키 추가 (`coding`, `codingLearn`)

## 3. 코딩 학습 외부 링크 연결
- **파일**: `src/config/site.js`, `src/components/layout/Navbar.jsx`
- **내용**: 코딩학습 드롭다운에 `코딩 학습` 메뉴 추가, `https://coding.dreamitbiz.com/` 외부 링크 연결
- **구현**: 메뉴 아이템에 `external: true` 플래그 추가, Navbar에서 해당 플래그가 있으면 `<a target="_blank">` 태그로 렌더링하도록 수정

## 4. 과목 카드 카테고리 재분류
- **파일**: `src/config/site.js`, `src/pages/Home.jsx`, `src/utils/translations.js`
- **내용**: 상단 메뉴 개편에 맞춰 과목 카드의 `category` 재분류
- **세부 내용**:
  - `categories` 배열에 `coding` 카테고리 추가
  - HTML 학습, Web 학습, React 학습의 `category`를 `computer` → `coding`으로 변경
  - 과목 순서 재배치: 컴퓨터전공(DB, 알고리즘, 자료구조) → 코딩학습(HTML, Web, React)
  - 홈 페이지 카테고리 설명 키 추가 (`categoryCodingDesc`)
  - 컴퓨터전공 카테고리 설명 업데이트 (DB, 알고리즘, 자료구조 중심)
  - Courses 페이지 `categoryTitle` 맵에 `coding` 추가

## 변경 파일 목록
| 파일 | 변경 |
|------|------|
| `src/config/site.js` | categories 배열에 coding 추가, 과목 category 재분류, 순서 재배치 |
| `src/utils/translations.js` | 메뉴명 변경, 코딩학습 번역 키/카테고리 설명 추가 (ko/en) |
| `src/components/layout/Navbar.jsx` | 외부 링크 `<a>` 태그 렌더링 지원 |
| `src/pages/Home.jsx` | CATEGORY_DESC_KEYS에 coding 키 추가 |
